# RavenRecon

IL2CPP class dumper. Injects into `com.AlfaBravo.CombatMaster` and writes an
IL2CPP class/field/method dump plus PNG page renders to the app container.

## Output

Written to the app's Documents container:
